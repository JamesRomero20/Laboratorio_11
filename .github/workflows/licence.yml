name: License

on:
  push:
    branches:
      - master
  pull_request:

jobs:
  license-check:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v3

      - name: Run ClearlyDefined Check
        uses: clearlydefined/gh-action@main


