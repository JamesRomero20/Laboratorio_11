name: License 

on:
  push:
    branches:
      - master
  pull_request:

jobs:
  license-check:
    name: License Compliance Check
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v3

      - name: Verify Repository License
        uses: actions/licensee-action@v1
