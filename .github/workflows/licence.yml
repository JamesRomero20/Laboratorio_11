name: License 

on:
  push:
    branches:
      - main
      - 'release/*'
      - master
  pull_request:

jobs:
  license-check:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v3

      - name: Run Ortoo License Check
        uses: ortoo/license-check@v1
